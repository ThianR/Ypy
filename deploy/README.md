# Despliegue inicial

Copiar `.env.example` fuera del repositorio y reemplazar sus valores mediante el gestor de secretos del entorno. El usuario de aplicación PostgreSQL debe ser distinto del usuario de migraciones. El archivo de servicio es una plantilla: requiere rutas, usuario, HTTPS y política de respaldos definidos por el piloto.

