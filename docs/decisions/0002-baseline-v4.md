# ADR 0002 — Baseline V4

`db/migrations/0001_baseline_v4.sql` es una copia exacta de `Genesis/creacionV4.sql`. Su hash SHA-256 se mantiene en el archivo `.sha256` contiguo. Las migraciones posteriores no modifican este archivo.

