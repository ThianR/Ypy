# Estado de ejecución

| Tarea | Estado | Evidencia |
|---|---|---|
| E00 | En progreso | Esqueleto en `cmd/`, `internal/`, `web/` y `contracts/`; backend compilado y probado con runtime portable local |
| E01 | En progreso | Manifiesto inicial de tablas/funciones y validación ejecutable `scripts/check-boundaries.ps1`; revisión funcional de funciones cruzadas pendiente |
| E02 | En progreso | Go/Vue/Vite, baseline V4, OpenAPI y JSON Schema POS versionados; pruebas/compilaciones aprobadas; runner reproducible de migraciones SQL validado en laboratorio; validación automática de `pos-operation` y `event-envelope` ejecutada por `scripts/validate-contracts.py` |
| E03 | En progreso | Adaptadores Dexie y Go/SQLite, contrato común e impresión simulada implementados; transacción Dexie y ACK/conflicto por hash probados; integración central y periféricos reales pendientes |
| F01 | En progreso | Contexto, sesión expirable, autenticación PostgreSQL mediante `crypt()`, endpoint de login scoped, identidad completa, IDs positivos y permisos por ámbito implementados con pruebas; `SessionStore` persistente sobre `ypy_usuario_sesion`, creación/revocación y middleware `X-Session-ID` integrable agregados y probados; al cargar una sesión PostgreSQL se verifican usuario, empresa, sucursal y terminal activos y se cargan permisos efectivos desde roles V4; `YPY_REQUIRE_SESSION=1` protege rutas `/api` cuando hay PostgreSQL; migraciones `0002` y `0006` agregadas; alta controlada de terminales y proveedor externo pendientes |
| F02 | En progreso | Aritmética decimal exacta Go/TypeScript unificada a escala fija de 6 posiciones, con rechazo de formatos inválidos y fracciones; adaptador PostgreSQL carga snapshots efectivos y persiste nuevas cotizaciones sin sobrescribir históricos; equivalencia SQL y conciliación completa pendientes |
| P05 | En progreso | Conversión offline exacta con caché Dexie versionada de snapshots; endpoint protegido `/api/v1/maestros/quotes` expone cotización V4 efectiva por fecha y ámbito de empresa para bootstrap offline; conciliación central y cierre completo pendientes |
| F03 | En progreso | Resolver de barras/presentaciones, buscador POS, filtro de inactivos y precios versionados por fecha; `BarcodeStore` PostgreSQL y endpoints `/api/v1/catalog/barcodes`, `/api/v1/catalog/items` y `/api/v1/catalog/prices` exponen únicamente datos activos/vigentes con ámbito de empresa; CRUD, impuestos y persistencia completa V4 pendientes |
| F04 | En progreso | Asignador de bloques exclusivos en Go y adaptador PostgreSQL sobre `gs_reservar_rango`/`gs_asignar_numero`, con idempotencia delegada a V4, pruebas de terminal, agotamiento y vencimiento; permisos firmados y flujo de alta controlada pendientes |
| V01 | En progreso | Libro de existencias inicial con recepción idempotente, conflicto explícito ante reutilización de ID con contenido distinto y consulta de saldo actual; adaptador PostgreSQL lee `gi_existencia`/`gi_posicion` con ámbito de empresa y pruebas parametrizadas; costo/kardex V4 y persistencia de recepciones pendientes |
| V02 | En progreso | Apertura, cobros y cierre básico de caja con decimales exactos y normalización de medios; adaptador PostgreSQL y endpoints `/api/v1/cash/open`/`close` crean aperturas y delegan cierre/arquéo a `gf_cerrar_caja`, con validación de ámbito y JSON; cobros persistentes, monedas y permisos reales pendientes |
| V03 | En progreso | Workflow de venta contado con validación de comando/total, número, cobro, salida de stock e idempotencia; endpoint comercial conectado a `gp_integrar_venta`; ventas POS con `formas` utilizan `gp_integrar_venta_cobro` de la migración `0007_venta_cobro.sql` para venta y recibo en una transacción; auditoría, validación PostgreSQL real y ticket pendientes |
| V04 | En progreso | Flujo Vue POS con búsqueda, carrito, suma decimal exacta, pagos y doble confirmación explícita antes de crear la operación, persistencia Dexie, ACK obligatorio y smoke HTTP reproducible; numeración real, ticket y framework E2E pendientes |
| P01 | En progreso | Adaptador Dexie/PWA con venta+cobro+stock en transacción; errores de cuota de IndexedDB se normalizan como `STORAGE_QUOTA_EXCEEDED` para hacer visible la insuficiencia de almacenamiento; interfaz visual completa pendiente |
| P03 | En progreso | ACK local y endpoint central con validación, persistencia inbox PostgreSQL, `EnqueueAccepted` durable, deduplicación UUID/hash con hash canónico JSON, normalización para idempotencia equivalente, ACK consistente e IDs positivos de empresa/terminal, secuencia obligatoria, conflicto y propagación de contexto HTTP/DB implementados; contrato comercial completo pendiente |
| P04 | En progreso | Secuenciador, snapshots Dexie, cursor monotónico local/central con validación de ámbito, reintentos con backoff acotado, cliente de sincronización, fallback de caché offline y ACK obligatorio validado por hash implementados; snapshots insuficientes abortan sin cantidades negativas; resolución auditada y cobertura E2E pendientes |
| P07 | En progreso | Simulador determinista por semilla conectado al secuenciador real y con gaps/entregas verificables; inyección reproducible de drops y duplicados, reenvío de drops, conteo de recuperados, traza determinista de estados, serialización JSON y validación formal de conciliación final agregados; integración E2E pendiente |
| C01 | En progreso | Orden y recepciones parciales con identidad, límite de cantidad e idempotencia; `PostgresStore.CreateHeader`, `AddDetail` y `Confirm` persisten cabecera/detalle V4 y delegan confirmación/stock a `gs_confirmar`; reintentos y conflictos controlados; factura y vínculos completos pendientes |
| C02 | En progreso | Costo promedio móvil exacto y estado de costo pendiente implementados; su resolución exige costo válido y motivo explícito, con pruebas; diferencias V4, trazabilidad y persistencia pendientes |
| C03 | En progreso | Series, vencimientos con límite seguro, identidad de lote y rechazo de series repetidas en una venta, y conversión exacta de presentaciones a unidad base implementados y probados; persistencia V4 pendiente |
| C08 | En progreso | Parser Go/TypeScript de códigos de peso/precio con prioridad exacta/prefijo, reglas malformadas ignoradas, campos de ancho fijo limitados a dígitos ASCII, valores cero rechazados y decimales fuera del ancho de valor invalidados; equivalencia offline probada; modelo posicional V4, lector PostgreSQL, endpoint, bootstrap y cachés Dexie/SQLite agregados; integración funcional completa pendiente |
| C04 | En progreso | Control de devoluciones parciales acumuladas, identidad e idempotencia con conflicto explícito de contenido implementado y probado; `ReturnStore` y `POST /api/v1/sales/returns` delegan la devolución física a `gv_devolver`; `CreditNoteStore` y `POST /api/v1/sales/credit-notes` crean y confirman notas de crédito; detalles financieros, costo original y validación PostgreSQL real pendientes |
| C05 | En progreso | Aplicación idempotente de pagos con identidad, conflicto explícito ante importe distinto y rechazo de sobreaplicación; `ReceiptStore` persiste cabecera y formas, `gp_integrar_venta_cobro` soporta cobro mixto atómico, y `CreditStore`/`POST /api/v1/sales/credits/apply` delegan aplicaciones a `gf_usar_credito`; multimoneda y validación PostgreSQL real pendientes |
| C06 | En progreso | Transferencia de stock con ruta, cantidad, identidad e idempotencia implementadas y probadas; reutilización del ID con ruta o cantidad distinta se rechaza sin modificar stock; despacho, entrega y persistencia V4 pendientes |
| C07 | En progreso | Conteo físico con identidad, referencia, diferencia, motivo normalizado y aprobación de supervisor de uso único, protegida contra aprobaciones concurrentes; ajuste V4 y pendientes offline siguen pendientes |
| C09 | En progreso | Movimientos de caja con identidad, sesión, tipo, importe, motivo normalizado e idempotencia con conflicto de contenido implementados y expuestos por HTTP; migración `0009_cierre_movimientos_caja.sql` incorpora ingresos y retiros al esperado del arqueo; multimoneda, cambio y validación PostgreSQL real pendientes |
| Operación | En progreso | Endpoints `/health` y `/ready`, smoke HTTP reproducible, propagación de contexto HTTP/DB y encolado durable de eventos aceptados integrado al endpoint POS agregados; diagnóstico de dependencias, pendientes y rangos requiere integración PostgreSQL/terminal |
| R03 | En progreso | Plantilla de configuración, servicio de ejemplo, empaquetado con manifiesto versionado/SHA-256, lista de archivos requeridos, verificación inversa del ZIP y benchmark reproducible de idempotencia POS (`scripts/bench.ps1`) agregados; firma criptográfica, respaldos operativos, objetivos acordados y matriz de carga pendientes |
| R01 | En progreso | Contrato JSON versionado de documento fiscal, adaptador con estados `PENDING`/`ACCEPTED`/`REJECTED`, entrega permitida solo con aceptación y autoridad, evidencia obligatoria de autoridad/rechazo, payload JSON objeto, empresa positiva, reintento idempotente con conflicto explícito ante contenido distinto y estado final inmutable implementado; verificación de modalidad/habilitación vigente, integración elegida y pruebas en entorno autorizado pendientes |
| R02 | En progreso | Agregación y exportación CSV determinista de ventas, existencias/kardex y caja, workbook `.xlsx` y PDF textual reproducibles para ventas; reportes completos, gráficos y aislamiento V4 pendientes |
| P02 | En progreso | Persistencia SQLite, validación de operaciones, idempotencia, cola de impresión persistente y sincronizador HTTP continuo con tres reintentos acotados, backoff cancelable, validación SHA-256 del `content_hash`, recuperación `SENDING`→`PENDING` y ACK `APPLIED`; intervalo y apagado limpio configurables; control de acceso y sincronización central completa pendientes |
| P06 | En progreso | Comprobación de integridad, backup y restauración SQLite agregados y probados; operaciones `SENDING` se recuperan a `PENDING` al reiniciar para reenvío idempotente; índice único local impide reutilizar números; actualización coordinada y periféricos reales pendientes |
| F06 | En progreso | Migración `0004_auditoria_contexto.sql` amplía `gs_auditoria` con operación, terminal, instalación, motivo y correlación; entradas rechazadas explícitas con motivo y redacción implementadas; autorización real y auditoría transaccional pendientes |
| F05 | En progreso | Middleware de `X-Request-ID`, errores JSON estables, envelope de eventos validado, `EnqueueAccepted` para salida durable, outbox PostgreSQL y worker con ACK posterior a publicación implementados/probados; los fallos de publicación persisten `reintentos`/`publicado_error`, el worker rechaza entradas inválidas y `Pending` rechaza payloads no JSON; persistencia adicional V4 y correlation/operation logs estructurados pendientes |
| UI01-UI03 | En progreso | Plan de interfaz versionado en `docs/planes/08_INTERFAZ_FORMULARIOS_Y_PERSONALIZACION.md`; paleta semántica configurable, densidad, adaptación responsive y `useStandardForm` implementados; migración completa de tablas, filtros y preferencias centralizadas pendiente |
